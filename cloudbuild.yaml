steps:
  # Install
  - name: 'gcr.io/cloud-builders/npm'
    args: ['install']

  # Add Config
  - name: 'gcr.io/cloud-builders/npm'
    args: ['run', 'config']

  # Build
  - name: 'gcr.io/cloud-builders/npm'
    args: ['run', 'build']

  # Deploy
  - name: 'gcr.io/$PROJECT_ID/firebase'
    args: ['deploy']
